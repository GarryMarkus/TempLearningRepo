sum of arrray:
accumulate(arr_name, arr_name + length, initial sum) 

arr_name will point to the first element of the array and the second parameter "arr_name + length" will tell the number of more elements to include and not the index values


max of array:
*max_element(arr_name, arr_name + length); //what does this * mark tells
The asterisk (*) in *max_element(arr_name, arr_name + length) means dereferencing the pointer returned by max_element. This gives the actual value of the maximum element in the array—not just its address.

min of array:
*min_element(arr_name, arr_name + length); //what does this * mark tells

printing values till a fixed places of decimal:
cout << fixed << setprecision(2) << value; // Outputs: 5.83 // only "value" is a variable and other parameters are fixed

