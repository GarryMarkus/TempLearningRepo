#include <bits/stdc++.h>
using namespace std;

int main(){
    string name;
    string age;
    string rollNumber;

    getline(cin, name);
    getline(cin, age);
    getline(cin, rollNumber);

    cout << name << " " << age << " " << rollNumber;
}